<% @talking_videos.each.with_index(1) do |talking_video, i| %>
  <div class="mt-5">
    <div class="text-center">
      <p><%= "Lv.#{i}： #{talking_video.title}" %></p>
        <iframe width="560" height="315" src="<%=talking_video.url%>" frameborder="0" allow="accelerometer; autoplay; encrypted-media; gyroscope; picture-in-picture" allowfullscreen></iframe>
    </div>
  </div>
<% end %>