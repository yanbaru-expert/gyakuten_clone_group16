<% flash.each do |key, value| %>
  <% key = 'info' %>
  <% if alert = notice %>
    <% key.sub!(/info/, 'info') %>
  <% else %>
    <% key.sub!(/info/, 'danger') %>
  <% end %>
  <%= content_tag :div, value, class: "alert alert-#{key}" %>
<% end %>
